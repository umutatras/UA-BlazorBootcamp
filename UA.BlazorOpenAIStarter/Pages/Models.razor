@page "/models/{ModelName}"
@*@page "/models"*@
@inject NavigationManager Navigation



@if (ModelName == "chatgpt")
{
    <button class="btn btn-danger mb-3" @onclick="@(()=>Navigation.NavigateTo("/models/dall-e"))">Dall-e</button>
    <ChatGpt></ChatGpt>
}
else
{
    <button class="btn btn-danger mb-3" @onclick="@(()=>Navigation.NavigateTo("/models/chatgpt"))">ChatGpt</button>
    <Dalle></Dalle>
}


@code {
    [Parameter]
    public string ModelName { get; set; }
    //    [SupplyParameterFromQuery(Name ="modelName")] queryString olarak alabilmemizi sağlıyor
    //[Parameter]
    //    public string ModelName { get; set; } 
}
